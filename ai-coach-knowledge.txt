:root {
  --navy: #0b1736;
  --blue: #153b8e;
  --lime: #c7f134;
  --cream: #f6f4ed;
  --white: #ffffff;
  --text: #182033;
  --muted: #667085;
  --radius: 20px;
  --shadow: 0 20px 50px rgba(11, 23, 54, 0.12);
}

* { box-sizing: border-box; }
html { scroll-behavior: smooth; }
[id] { scroll-margin-top: 96px; }
body { margin: 0; color: var(--text); background: var(--cream); font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; line-height: 1.6; }
a { color: inherit; text-decoration: none; }
.sr-only { position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0; }

.site-header { position: sticky; top: 0; z-index: 10; display: flex; align-items: center; justify-content: space-between; min-height: 76px; padding: 0 6vw; color: var(--white); background: rgba(11, 23, 54, 0.96); backdrop-filter: blur(14px); }
.logo { display: inline-flex; align-items: center; gap: 10px; font-weight: 800; letter-spacing: -0.02em; }
.logo-mark { display: grid; place-items: center; width: 34px; height: 34px; color: var(--navy); background: var(--lime); border-radius: 50% 50% 50% 12%; font-style: italic; }
.main-nav { display: flex; align-items: center; gap: clamp(12px, 1.6vw, 28px); font-size: 0.9rem; font-weight: 650; }
.main-nav a { --nav-x: 0px; --nav-y: 0px; --nav-lift: 0px; position: relative; isolation: isolate; display: inline-flex; align-items: center; justify-content: center; overflow: hidden; transform: translate3d(var(--nav-x), calc(var(--nav-y) - var(--nav-lift)), 0); transition: color 180ms ease, background 180ms ease, box-shadow 180ms ease, transform 180ms ease; will-change: transform; }
.main-nav a:not(.nav-cta) { padding: 9px 7px; border-radius: 11px; }
.main-nav a:not(.nav-cta)::before { content: ""; position: absolute; z-index: -1; inset: 2px; background: rgba(199, 241, 52, .1); border: 1px solid rgba(199, 241, 52, .18); border-radius: 9px; opacity: 0; transform: scale(.72); transition: opacity 180ms ease, transform 180ms ease; }
.main-nav a:not(.nav-cta)::after { content: ""; position: absolute; right: 7px; bottom: 4px; left: 7px; height: 2px; background: var(--lime); border-radius: 999px; transform: scaleX(0); transform-origin: right; transition: transform 220ms ease; }
.main-nav a:not(.nav-cta):hover, .main-nav a:not(.nav-cta).is-active, .main-nav a:not(.nav-cta)[aria-current] { --nav-lift: 2px; color: var(--lime); }
.main-nav a:not(.nav-cta):hover::before, .main-nav a:not(.nav-cta).is-active::before, .main-nav a:not(.nav-cta)[aria-current]::before { opacity: 1; transform: scale(1); }
.main-nav a:not(.nav-cta):hover::after, .main-nav a:not(.nav-cta).is-active::after, .main-nav a:not(.nav-cta)[aria-current]::after { transform: scaleX(1); transform-origin: left; }
.main-nav a:active { --nav-lift: -1px; }
.main-nav a:focus-visible { outline: 3px solid var(--lime); outline-offset: 3px; }
.nav-cta { padding: 11px 18px; color: var(--navy); background: var(--lime); border-radius: 999px; box-shadow: 0 7px 20px rgba(199, 241, 52, .16); font-weight: 850; }
.nav-cta::before { content: ""; position: absolute; top: -90%; bottom: -90%; left: -45%; width: 30%; background: linear-gradient(90deg, transparent, rgba(255, 255, 255, .78), transparent); transform: translateX(-240%) rotate(18deg); transition: transform 600ms ease; pointer-events: none; }
.nav-cta:hover { --nav-lift: 3px; color: var(--navy); background: var(--white); box-shadow: 0 12px 30px rgba(199, 241, 52, .3), 0 0 0 4px rgba(199, 241, 52, .13); }
.nav-cta:hover::before { transform: translateX(650%) rotate(18deg); }
.menu-toggle { display: none; padding: 8px; background: none; border: 0; }
.menu-toggle > span:not(.sr-only):not(.site-interaction-layer):not(.site-button-ripple) { display: block; width: 24px; height: 2px; margin: 5px; background: var(--white); }

/* Shared button interactions */
.site-interactive { --interaction-x: 50%; --interaction-y: 50%; position: relative; isolation: isolate; overflow: hidden; scale: 1; transition-property: color, background-color, border-color, box-shadow, filter, scale, transform; transition-duration: 180ms; transition-timing-function: ease; -webkit-tap-highlight-color: transparent; }
.site-interactive:hover:not([disabled]):not([aria-disabled="true"]) { filter: brightness(1.045) saturate(1.04); scale: 1.015; }
.site-interactive.is-interaction-pressed:not([disabled]):not([aria-disabled="true"]) { filter: brightness(.96); scale: .965; }
.site-interactive.is-key-activated:not([disabled]):not([aria-disabled="true"]) { animation: site-key-activate 380ms ease; }
.site-interactive:focus-visible { outline: 3px solid var(--lime); outline-offset: 3px; }
.site-interactive[disabled], .site-interactive[aria-disabled="true"] { scale: 1; }
.site-interaction-layer { position: absolute; z-index: 5; inset: 0; background: radial-gradient(circle 85px at var(--interaction-x) var(--interaction-y), rgba(255, 255, 255, .3), rgba(199, 241, 52, .12) 34%, transparent 72%); opacity: 0; transition: opacity 180ms ease; pointer-events: none; }
.site-interactive:hover > .site-interaction-layer, .site-interactive:focus-visible > .site-interaction-layer { opacity: 1; }
.site-button-ripple { position: absolute; z-index: 6; width: var(--ripple-size); height: var(--ripple-size); background: rgba(255, 255, 255, .48); border: 1px solid rgba(199, 241, 52, .5); border-radius: 50%; mix-blend-mode: soft-light; transform: translate(-50%, -50%) scale(0); animation: site-button-ripple 560ms ease-out forwards; pointer-events: none; }
@keyframes site-button-ripple { 65% { opacity: .55; } 100% { opacity: 0; transform: translate(-50%, -50%) scale(1); } }
@keyframes site-key-activate { 40% { scale: .965; } 75% { scale: 1.035; } }

.hero { min-height: calc(100vh - 76px); display: grid; grid-template-columns: 1.15fr 0.85fr; align-items: center; gap: 6vw; padding: 8vw 6vw; color: var(--white); background: radial-gradient(circle at 80% 30%, rgba(199, 241, 52, 0.16), transparent 24%), linear-gradient(135deg, var(--navy), #102b68); overflow: hidden; }
.hero-content { max-width: 760px; }
.eyebrow { margin: 0 0 14px; color: #779103; font-size: 0.76rem; font-weight: 850; letter-spacing: 0.16em; text-transform: uppercase; }
.hero .eyebrow, .contact .eyebrow { color: var(--lime); }
h1, h2, h3, p { margin-top: 0; }
h1 { margin-bottom: 22px; font-size: clamp(3.25rem, 7vw, 6.6rem); line-height: 0.95; letter-spacing: -0.065em; }
h1 span { color: var(--lime); }
.hero-copy { max-width: 620px; color: #cbd5e1; font-size: clamp(1rem, 2vw, 1.25rem); }
.hero-actions { display: flex; flex-wrap: wrap; gap: 14px; margin: 34px 0 52px; }
.button { display: inline-flex; justify-content: center; padding: 14px 22px; border: 1px solid transparent; border-radius: 999px; font-weight: 750; transition: transform 160ms ease, box-shadow 160ms ease; }
.button:hover { transform: translateY(-2px); box-shadow: 0 10px 30px rgba(0, 0, 0, 0.18); }
.button-primary { color: var(--navy); background: var(--lime); }
.button-secondary { border-color: rgba(255, 255, 255, 0.35); }
.hero-stats { display: flex; gap: clamp(28px, 5vw, 64px); }
.hero-stats div { display: flex; flex-direction: column; }
.hero-stats strong { color: var(--lime); font-size: 1.5rem; }
.hero-stats span { color: #aebbd2; font-size: 0.8rem; }
.hero-visual { position: relative; min-height: 450px; border-radius: 45% 45% 16px 16px; background: linear-gradient(145deg, rgba(255,255,255,.13), rgba(255,255,255,.02)), repeating-linear-gradient(45deg, transparent 0 30px, rgba(199,241,52,.06) 30px 32px); border: 1px solid rgba(255,255,255,.14); }
.hero-visual::before { content: "PEAK"; position: absolute; inset: 50% auto auto 50%; transform: translate(-50%, -50%) rotate(-8deg); color: rgba(255,255,255,.07); font-size: clamp(5rem, 12vw, 11rem); font-weight: 950; letter-spacing: -.1em; }
.visual-card { position: absolute; right: -20px; bottom: 30px; display: flex; flex-direction: column; width: min(310px, 85%); padding: 25px; color: var(--navy); background: var(--lime); border-radius: var(--radius); box-shadow: var(--shadow); transform: rotate(-3deg); }
.visual-card span, .visual-card small { font-weight: 650; opacity: .72; }
.visual-card strong { margin: 5px 0; font-size: 1.35rem; }

.section { padding: 100px 6vw; }
.section-heading { max-width: 680px; margin-bottom: 48px; }
h2 { margin-bottom: 18px; color: var(--navy); font-size: clamp(2.2rem, 4vw, 3.8rem); line-height: 1.05; letter-spacing: -0.045em; }
.section-heading > p:last-child, .about > div > p { color: var(--muted); }
.program-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 22px; }
.program-card { --spot-x: 50%; --spot-y: 50%; --tilt-x: 0deg; --tilt-y: 0deg; position: relative; isolation: isolate; display: flex; flex-direction: column; min-height: 360px; padding: 30px; overflow: hidden; background: var(--white); border: 1px solid rgba(21, 59, 142, .1); border-radius: 24px; box-shadow: 0 12px 38px rgba(11, 23, 54, .08); transform: perspective(900px) rotateX(var(--tilt-x)) rotateY(var(--tilt-y)); transition: color 220ms ease, background 220ms ease, border-color 220ms ease, box-shadow 220ms ease, transform 220ms ease; }
.program-card::before { content: ""; position: absolute; z-index: -1; inset: 0; background: radial-gradient(circle 230px at var(--spot-x) var(--spot-y), rgba(199, 241, 52, .22), transparent 68%); opacity: 0; transition: opacity 220ms ease; pointer-events: none; }
.program-card::after { content: ""; position: absolute; top: 0; left: 30px; width: 54px; height: 5px; background: var(--lime); border-radius: 0 0 999px 999px; transition: width 260ms ease; }
.program-card:hover::before, .program-card:focus-within::before { opacity: 1; }
.program-card:hover::after, .program-card:focus-within::after, .program-card.is-selected::after { width: calc(100% - 60px); }
.program-card.is-selected { color: var(--white); background: linear-gradient(145deg, #1b49aa, var(--blue)); border-color: rgba(199, 241, 52, .72); box-shadow: 0 24px 55px rgba(21, 59, 142, .24); }
.program-card-top { display: flex; align-items: center; justify-content: space-between; gap: 18px; }
.card-number { color: #759008; font-family: "Arial Black", Inter, sans-serif; font-size: .75rem; font-weight: 900; letter-spacing: .12em; }
.program-mark { display: grid; place-items: center; width: 48px; height: 48px; color: var(--navy); background: #eef8c9; border: 1px solid rgba(119, 145, 3, .24); border-radius: 15px; font-family: "Arial Black", Inter, sans-serif; font-size: .75rem; letter-spacing: .04em; transform: rotate(4deg); transition: color 220ms ease, background 220ms ease, transform 220ms ease; }
.program-card:hover .program-mark, .program-card:focus-within .program-mark { transform: rotate(-5deg) scale(1.08); }
.program-card.is-selected .card-number { color: var(--lime); }
.program-card.is-selected .program-mark { color: var(--navy); background: var(--lime); border-color: var(--lime); }
.program-card h3 { margin: 30px 0 11px; color: var(--navy); font-family: "Trebuchet MS", Inter, sans-serif; font-size: clamp(1.55rem, 2.2vw, 1.9rem); font-weight: 900; line-height: 1.08; letter-spacing: -.045em; }
.program-card p { margin-bottom: 22px; color: var(--muted); line-height: 1.68; }
.program-card.is-selected h3 { color: var(--white); }
.program-card.is-selected p { color: #d4def2; }
.program-facts { display: flex; flex-wrap: wrap; gap: 8px; margin-top: auto; margin-bottom: 24px; }
.program-facts span { padding: 6px 10px; color: #5f7308; background: #f1f7da; border-radius: 999px; font-size: .72rem; font-weight: 850; letter-spacing: .025em; }
.program-card.is-selected .program-facts span { color: var(--white); background: rgba(255, 255, 255, .12); }
.program-select { display: flex; align-items: center; justify-content: space-between; width: 100%; padding: 13px 16px; color: var(--navy); background: #edf2fb; border: 0; border-radius: 13px; font: inherit; font-weight: 850; cursor: pointer; transition: color 180ms ease, background 180ms ease, transform 180ms ease; }
.program-select:hover { color: var(--navy); background: var(--lime); transform: translateY(-2px); }
.program-select:focus-visible { outline: 3px solid var(--lime); outline-offset: 3px; }
.program-card.is-selected .program-select { color: var(--navy); background: var(--lime); }
.program-select span { font-size: 1.1rem; transition: transform 180ms ease; }
.program-select:hover span { transform: translateX(4px); }
.program-selection { display: flex; align-items: center; justify-content: space-between; gap: 34px; margin-top: 26px; padding: 24px 28px; color: var(--white); background: var(--navy); border-radius: 20px; box-shadow: var(--shadow); }
.program-selection > div { max-width: 730px; }
.selection-label { display: block; margin-bottom: 3px; color: var(--lime); font-size: .68rem; font-weight: 900; letter-spacing: .14em; text-transform: uppercase; }
.program-selection strong { display: block; font-family: "Trebuchet MS", Inter, sans-serif; font-size: 1.35rem; letter-spacing: -.025em; }
.program-selection p { margin: 3px 0 0; color: #b8c6df; font-size: .9rem; line-height: 1.5; }
.program-selection .button { flex: 0 0 auto; gap: 8px; white-space: nowrap; }
@media (hover: hover) and (pointer: fine) {
  .program-card:hover { border-color: rgba(119, 145, 3, .48); box-shadow: 0 28px 62px rgba(11, 23, 54, .17); transform: perspective(900px) translateY(-10px) rotateX(var(--tilt-x)) rotateY(var(--tilt-y)); }
}
@media (prefers-reduced-motion: reduce) {
  .main-nav a, .main-nav a::before, .main-nav a::after,
  .program-card, .program-card::before, .program-card::after, .program-mark, .program-select, .program-select span { transition: none; }
}
.about { display: grid; grid-template-columns: 1fr 1fr; gap: 9vw; background: var(--white); }
.check-list { display: grid; gap: 13px; margin: 28px 0 0; padding: 0; list-style: none; font-weight: 700; }
.check-list li::before { content: "✓"; display: inline-grid; place-items: center; width: 25px; height: 25px; margin-right: 11px; color: var(--navy); background: var(--lime); border-radius: 50%; }
.coaches { min-height: 400px; background: linear-gradient(120deg, #e9eddf, var(--cream)); }
.contact { display: grid; grid-template-columns: .8fr 1.2fr; align-items: start; gap: clamp(40px, 8vw, 110px); padding: 90px 6vw; color: var(--white); background: var(--blue); }
.contact h2 { color: var(--white); }
.contact p:not(.eyebrow) { color: #c8d4ef; }
.contact-intro { position: sticky; top: 116px; }
.contact-note { display: flex; flex-direction: column; gap: 4px; margin-top: 34px; padding: 20px; color: var(--navy); background: var(--lime); border-radius: 16px; }
.contact-note span { font-size: .9rem; }
.contact-form { display: grid; gap: 20px; padding: clamp(24px, 4vw, 42px); color: var(--text); text-align: left; background: var(--white); border-radius: var(--radius); box-shadow: var(--shadow); }
.contact-form.plan-loaded { outline: 3px solid var(--lime); outline-offset: 5px; box-shadow: 0 24px 65px rgba(199, 241, 52, .2); }
.form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 18px; }
.form-field { display: grid; gap: 7px; }
.form-field label { color: var(--navy); font-size: .86rem; font-weight: 800; }
.form-field input, .form-field select, .form-field textarea { width: 100%; padding: 13px 14px; color: var(--text); background: #f8fafc; border: 1px solid #d7deea; border-radius: 10px; font: inherit; outline: none; transition: border-color 160ms ease, box-shadow 160ms ease; }
.form-field textarea { min-height: 135px; resize: vertical; }
.form-field input:focus, .form-field select:focus, .form-field textarea:focus { border-color: var(--blue); box-shadow: 0 0 0 3px rgba(21, 59, 142, .12); }
.form-submit { justify-self: start; min-width: 160px; color: var(--navy); background: var(--lime); border: 0; cursor: pointer; }
.form-submit:disabled { cursor: wait; opacity: .7; transform: none; box-shadow: none; }
.form-status { min-height: 1.5em; margin: -8px 0 0; color: #247a47 !important; font-size: .92rem; font-weight: 750; }
.form-status.error { color: #b42318 !important; }
footer { display: flex; align-items: center; justify-content: space-between; gap: 30px; padding: 34px 6vw; color: #aebbd2; background: var(--navy); font-size: .82rem; }
footer p { margin: 0; }

.coach-launcher { position: fixed; right: 24px; bottom: 24px; z-index: 30; display: flex; align-items: center; gap: 10px; padding: 10px 18px 10px 10px; color: var(--navy); background: var(--lime); border: 0; border-radius: 999px; box-shadow: 0 12px 35px rgba(11, 23, 54, .28); font: inherit; font-weight: 850; cursor: pointer; transition: transform 160ms ease; }
.coach-launcher:hover { transform: translateY(-2px); }
.coach-launcher-icon { display: grid; place-items: center; width: 38px; height: 38px; color: var(--white); background: var(--navy); border-radius: 50%; font-size: .72rem; }
.coach-assistant { position: fixed; right: 24px; bottom: 88px; z-index: 30; display: none; grid-template-rows: auto minmax(0, 1fr) auto; width: min(390px, calc(100vw - 32px)); height: min(590px, calc(100vh - 120px)); overflow: hidden; background: var(--white); border: 1px solid rgba(11, 23, 54, .12); border-radius: 22px; box-shadow: 0 24px 70px rgba(11, 23, 54, .3); }
.coach-assistant.open { display: grid; animation: coach-enter 180ms ease-out; }
@keyframes coach-enter { from { opacity: 0; transform: translateY(12px) scale(.98); } to { opacity: 1; transform: none; } }
.coach-header { display: flex; align-items: center; gap: 11px; padding: 17px; color: var(--white); background: var(--navy); }
.coach-avatar { display: grid; place-items: center; width: 40px; height: 40px; flex: 0 0 auto; color: var(--navy); background: var(--lime); border-radius: 50% 50% 50% 12%; font-weight: 900; }
.coach-header > div:nth-child(2) { display: flex; flex-direction: column; line-height: 1.25; }
.coach-header span { color: #b8c4d9; font-size: .76rem; }
.coach-header i { display: inline-block; width: 7px; height: 7px; margin-right: 4px; background: var(--lime); border-radius: 50%; }
.coach-close { margin-left: auto; padding: 5px 8px; color: var(--white); background: transparent; border: 0; font-size: 1.7rem; line-height: 1; cursor: pointer; }
.coach-messages { display: flex; flex-direction: column; gap: 12px; padding: 18px; overflow-y: auto; background: #f4f7fb; }
.coach-message { max-width: 86%; padding: 11px 14px; border-radius: 16px; font-size: .9rem; line-height: 1.5; white-space: pre-line; }
.assistant-message { align-self: flex-start; color: var(--text); background: var(--white); border-bottom-left-radius: 4px; box-shadow: 0 5px 18px rgba(11, 23, 54, .06); }
.user-message { align-self: flex-end; color: var(--white); background: var(--blue); border-bottom-right-radius: 4px; }
.coach-suggestions { display: flex; flex-wrap: wrap; gap: 7px; }
.coach-suggestions button { padding: 8px 10px; color: var(--blue); background: var(--white); border: 1px solid #cbd5e1; border-radius: 999px; font: inherit; font-size: .74rem; font-weight: 700; cursor: pointer; }
.coach-suggestions button:hover { border-color: var(--blue); }
.coach-input-form { display: flex; gap: 8px; padding: 13px; background: var(--white); border-top: 1px solid #e3e8f0; }
.coach-input-form input { min-width: 0; flex: 1; padding: 11px 13px; border: 1px solid #cfd7e4; border-radius: 999px; font: inherit; outline: none; }
.coach-input-form input:focus { border-color: var(--blue); box-shadow: 0 0 0 3px rgba(21, 59, 142, .1); }
.coach-input-form button { padding: 9px 15px; color: var(--navy); background: var(--lime); border: 0; border-radius: 999px; font: inherit; font-size: .82rem; font-weight: 850; cursor: pointer; }
.coach-action-link { display: block; margin-top: 10px; padding: 9px 12px; color: var(--navy); text-align: center; background: var(--lime); border-radius: 999px; font-size: .78rem; font-weight: 850; }

.main-nav a[aria-current="page"] { color: var(--lime); }
.faq-hero { padding: 90px 6vw 76px; color: var(--white); text-align: center; background: radial-gradient(circle at 18% 20%, rgba(199, 241, 52, .16), transparent 25%), linear-gradient(135deg, var(--navy), #102b68); }
.faq-hero h1 { max-width: 850px; margin: 0 auto 20px; font-size: clamp(3rem, 7vw, 6rem); }
.faq-hero > p:not(.eyebrow) { max-width: 670px; margin: 0 auto; color: #cbd5e1; font-size: 1.05rem; }
.faq-search { display: flex; align-items: center; gap: 12px; width: min(650px, 100%); margin: 36px auto 0; padding: 7px 8px 7px 18px; color: var(--navy); background: var(--white); border-radius: 999px; box-shadow: var(--shadow); }
.faq-search span { color: var(--blue); font-size: .78rem; font-weight: 850; text-transform: uppercase; }
.faq-search input { min-width: 0; flex: 1; padding: 11px; color: var(--text); background: transparent; border: 0; font: inherit; outline: none; }
.faq-content { min-height: 600px; padding: 85px 6vw 100px; }
.faq-content-heading { display: flex; align-items: end; justify-content: space-between; gap: 30px; max-width: 980px; margin: 0 auto 30px; }
.faq-content-heading h2 { margin-bottom: 0; }
.faq-content-heading > p { flex: 0 0 auto; margin: 0 0 8px; color: var(--muted); font-size: .88rem; font-weight: 750; }
.faq-filters { display: flex; flex-wrap: wrap; gap: 9px; max-width: 980px; margin: 0 auto 28px; }
.faq-filters button { padding: 9px 14px; color: var(--muted); background: var(--white); border: 1px solid #d8dee9; border-radius: 999px; font: inherit; font-size: .82rem; font-weight: 750; cursor: pointer; }
.faq-filters button.active { color: var(--navy); background: var(--lime); border-color: var(--lime); }
.faq-list { display: grid; gap: 12px; max-width: 980px; margin: 0 auto; }
.faq-item { overflow: hidden; background: var(--white); border: 1px solid #e0e5ed; border-radius: 15px; box-shadow: 0 7px 24px rgba(11, 23, 54, .04); }
.faq-question { display: flex; align-items: center; justify-content: space-between; gap: 20px; width: 100%; padding: 21px 24px; color: var(--navy); text-align: left; background: transparent; border: 0; font: inherit; font-weight: 800; cursor: pointer; }
.faq-question span { display: grid; gap: 3px; }
.faq-question small { color: #779103; font-size: .65rem; letter-spacing: .1em; text-transform: uppercase; }
.faq-question i { display: grid; place-items: center; width: 30px; height: 30px; flex: 0 0 auto; color: var(--navy); background: var(--lime); border-radius: 50%; font-size: 1.2rem; font-style: normal; }
.faq-answer { padding: 0 24px 22px; }
.faq-answer p { max-width: 820px; margin: 0; color: var(--muted); }
.faq-subscription-link { display: inline-flex; margin-top: 16px; padding: 10px 16px; color: var(--navy); background: var(--lime); border-radius: 999px; font-size: .8rem; font-weight: 850; }
.faq-subscription-link:hover { box-shadow: 0 7px 20px rgba(11, 23, 54, .12); transform: translateY(-1px); }
.faq-empty { max-width: 980px; margin: 40px auto; padding: 35px; color: var(--muted); text-align: center; background: var(--white); border-radius: var(--radius); }
.faq-empty strong { color: var(--navy); font-size: 1.2rem; }
.faq-empty p { margin: 6px 0 0; }
.faq-cta { display: flex; align-items: center; justify-content: space-between; gap: 40px; padding: 70px 8vw; color: var(--white); background: var(--blue); }
.faq-cta h2 { color: var(--white); }
.faq-cta p:not(.eyebrow) { max-width: 650px; margin-bottom: 0; color: #c8d4ef; }
.faq-cta .button { flex: 0 0 auto; }

.pricing-hero { padding: 90px 6vw 100px; color: var(--white); text-align: center; background: radial-gradient(circle at 75% 25%, rgba(199, 241, 52, .18), transparent 24%), linear-gradient(135deg, var(--navy), #102b68); }
.pricing-hero h1 { max-width: 900px; margin: 0 auto 22px; font-size: clamp(3rem, 7vw, 6rem); }
.pricing-hero h1 span { color: var(--lime); }
.pricing-hero > p:not(.eyebrow) { max-width: 650px; margin: 0 auto; color: #cbd5e1; font-size: 1.08rem; }
.pricing-section { padding: 90px 6vw 110px; }
.pricing-heading { margin-left: auto; margin-right: auto; text-align: center; }
.pricing-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 16px; max-width: 1300px; margin: 0 auto; }
.pricing-card { position: relative; display: flex; flex-direction: column; min-height: 390px; padding: 28px 24px; background: var(--white); border: 2px solid transparent; border-radius: var(--radius); box-shadow: 0 12px 35px rgba(11, 23, 54, .07); transition: transform 160ms ease, border-color 160ms ease; }
.pricing-card:hover { transform: translateY(-5px); }
.pricing-card.selected { border-color: var(--lime); }
.pricing-card.featured-plan { color: var(--white); background: var(--blue); transform: translateY(-10px); }
.pricing-card.featured-plan:hover { transform: translateY(-14px); }
.popular-badge { position: absolute; top: -13px; right: 18px; padding: 5px 11px; color: var(--navy); background: var(--lime); border-radius: 999px; font-size: .68rem; font-weight: 900; text-transform: uppercase; }
.plan-label { margin-bottom: 10px; color: #779103; font-size: .7rem; font-weight: 900; letter-spacing: .1em; text-transform: uppercase; }
.pricing-card h3 { margin-bottom: 12px; color: var(--navy); font-size: 1.5rem; }
.featured-plan h3 { color: var(--white); }
.plan-price { display: flex; align-items: flex-start; margin: 0 0 22px; color: var(--navy); font-size: 2.7rem; font-weight: 900; line-height: 1; letter-spacing: -.05em; }
.featured-plan .plan-price { color: var(--lime); }
.plan-price > span { margin: 4px 2px 0 0; font-size: 1rem; }
.plan-price small { align-self: flex-end; margin: 0 0 3px 5px; color: var(--muted); font-size: .7rem; font-weight: 700; letter-spacing: 0; }
.featured-plan .plan-price small, .featured-plan > p:not(.plan-label, .plan-price) { color: #c8d4ef; }
.pricing-card > p:not(.plan-label, .plan-price) { color: var(--muted); font-size: .9rem; }
.pricing-card button { width: 100%; margin-top: auto; padding: 12px; color: var(--navy); background: var(--lime); border: 0; border-radius: 999px; font: inherit; font-weight: 850; cursor: pointer; }
.checkout-section { display: grid; grid-template-columns: 1fr .8fr; align-items: center; gap: clamp(40px, 9vw, 120px); padding: 90px 8vw; background: var(--white); }
.checkout-copy > p:not(.eyebrow) { color: var(--muted); }
.checkout-card { display: grid; gap: 10px; padding: clamp(28px, 4vw, 44px); color: var(--white); background: var(--navy); border-radius: var(--radius); box-shadow: var(--shadow); }
.checkout-card > span { color: var(--lime); font-size: .72rem; font-weight: 900; letter-spacing: .1em; text-transform: uppercase; }
.checkout-card > strong { font-size: 1.8rem; }
.checkout-card > p { color: #cbd5e1; }
.checkout-card > small { color: #9cabc4; line-height: 1.5; }
.checkout-button { margin: 10px 0 4px; color: var(--navy); background: var(--lime); }
.checkout-button.disabled { opacity: .45; cursor: not-allowed; }
.custom-plan-cta { display: flex; align-items: center; justify-content: space-between; gap: 40px; padding: 70px 8vw; color: var(--white); background: var(--blue); }
.custom-plan-cta h2 { color: var(--white); }
.custom-plan-cta p:not(.eyebrow) { max-width: 700px; margin-bottom: 0; color: #c8d4ef; }
.custom-plan-cta .button { flex: 0 0 auto; }

.custom-plan-hero { display: grid; grid-template-columns: 1.3fr .7fr; align-items: center; gap: 8vw; padding: 90px 7vw; color: var(--white); background: radial-gradient(circle at 78% 30%, rgba(199, 241, 52, .2), transparent 25%), linear-gradient(135deg, var(--navy), #102b68); }
.custom-plan-hero h1 { font-size: clamp(3.1rem, 7vw, 6.2rem); }
.custom-plan-hero h1 span { color: var(--lime); }
.custom-plan-hero > div:first-child > p:not(.eyebrow) { max-width: 690px; color: #cbd5e1; font-size: 1.08rem; }
.custom-hero-stat { display: grid; gap: 10px; padding: 36px; color: var(--navy); background: var(--lime); border-radius: var(--radius); box-shadow: var(--shadow); transform: rotate(2deg); }
.custom-hero-stat strong { font-size: clamp(2rem, 4vw, 3.4rem); line-height: 1; }
.custom-hero-stat span { font-weight: 700; }
.discount-banner-section { padding: 90px 6vw; }
.discount-banner-frame { width: min(1200px, 100%); margin: 0 auto; }
.discount-banner-picture { display: block; width: 100%; }
.discount-banner-image { display: block; width: 100%; height: auto; object-fit: contain; border-radius: var(--radius); box-shadow: var(--shadow); }
.discount-banner-fallback { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2px; min-height: 390px; padding: 2px; overflow: hidden; color: var(--white); background: var(--lime); border-radius: var(--radius); box-shadow: var(--shadow); }
.discount-banner-fallback[hidden] { display: none; }
.discount-banner-fallback article { display: grid; place-content: center; gap: 22px; padding: 35px 20px; text-align: center; background: radial-gradient(circle at 50% 20%, #173f91, var(--navy) 70%); }
.discount-banner-fallback strong { color: var(--lime); font-size: clamp(3.5rem, 8vw, 7rem); line-height: .8; letter-spacing: -.07em; text-shadow: 0 5px 0 #07102a; }
.discount-banner-fallback strong span { display: block; margin-top: 16px; color: var(--white); font-size: .38em; letter-spacing: -.03em; }
.discount-banner-fallback small { padding: 10px 14px; color: var(--navy); background: var(--lime); border-radius: 8px; font-size: clamp(.85rem, 1.5vw, 1.25rem); font-weight: 900; }

.about-me-hero { display: grid; grid-template-columns: 1.2fr .8fr; align-items: center; gap: 8vw; min-height: calc(100vh - 76px); padding: 90px 7vw; color: var(--white); background: radial-gradient(circle at 80% 28%, rgba(199, 241, 52, .17), transparent 24%), linear-gradient(135deg, var(--navy), #102b68); }
.about-me-copy h1 { max-width: 850px; font-size: clamp(3.2rem, 7vw, 6.3rem); }
.about-me-copy h1 span { color: var(--lime); }
.about-me-copy > p:not(.eyebrow) { max-width: 700px; color: #cbd5e1; font-size: 1.08rem; }
.about-me-copy .button { margin-top: 18px; }
.founder-card { position: relative; display: grid; gap: 12px; padding: clamp(30px, 5vw, 52px); color: var(--navy); background: var(--lime); border-radius: 42% 42% var(--radius) var(--radius); box-shadow: var(--shadow); transform: rotate(2deg); }
.founder-avatar-frame { position: relative; width: min(290px, 88%); aspect-ratio: 1; margin: -8px auto 20px; padding: 7px; background: var(--navy); border-radius: 50%; box-shadow: 0 16px 35px rgba(11, 23, 54, .22); }
.founder-avatar { display: block; width: 100%; height: 100%; object-fit: cover; border: 3px solid var(--lime); border-radius: 50%; }
.founder-avatar[hidden] { display: none; }
.founder-monogram { display: grid; place-items: center; width: 100%; height: 100%; margin: 0; color: var(--lime); background: var(--navy); border-radius: 50%; font-size: 6rem; font-style: italic; font-weight: 900; }
.founder-monogram[hidden] { display: none; }
.founder-card > span { font-size: .72rem; font-weight: 900; letter-spacing: .11em; text-align: center; text-transform: uppercase; }
.founder-card > strong { font-size: clamp(1.6rem, 3vw, 2.4rem); line-height: 1.05; text-align: center; }
.founder-card > p { margin: 4px 0 0; text-align: center; }
.founder-story { display: grid; grid-template-columns: 1fr 1fr; gap: 9vw; background: var(--white); }
.story-body > p { color: var(--muted); }
.story-body blockquote { margin: 32px 0 0; padding: 24px 0 24px 24px; color: var(--navy); border-left: 5px solid var(--lime); font-size: 1.2rem; font-weight: 750; line-height: 1.55; }
.founder-vision { background: var(--navy); }
.founder-vision h2, .founder-vision h3 { color: var(--white); }
.founder-vision-intro { max-width: 800px; margin-bottom: 45px; }
.founder-vision-intro > p:last-child { color: #b8c4d9; }
.founder-vision-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 18px; }
.founder-vision-grid article { padding: 30px; background: rgba(255, 255, 255, .07); border: 1px solid rgba(255, 255, 255, .1); border-radius: var(--radius); }
.founder-vision-grid h3 { margin-bottom: 12px; }
.founder-vision-grid p { margin-bottom: 0; color: #b8c4d9; }
.academy-story { background: linear-gradient(120deg, #e9eddf, var(--cream)); }
.academy-story-intro { max-width: 760px; margin-bottom: 50px; }
.academy-story-intro > p:last-child { color: var(--muted); }
.academy-principles { display: grid; grid-template-columns: repeat(4, 1fr); gap: 18px; }
.academy-principles article { padding: 28px; background: var(--white); border-radius: var(--radius); box-shadow: 0 10px 30px rgba(11, 23, 54, .06); }
.academy-principles article > span { color: #779103; font-size: .72rem; font-weight: 900; }
.academy-principles h3 { margin: 35px 0 10px; color: var(--navy); }
.academy-principles p { margin-bottom: 0; color: var(--muted); font-size: .9rem; }
.about-approach { display: grid; grid-template-columns: .8fr 1.2fr; gap: 9vw; background: var(--white); }
.approach-steps { display: grid; gap: 14px; margin: 0; padding: 0; list-style: none; counter-reset: approach-step; }
.approach-steps li { position: relative; display: grid; grid-template-columns: 130px 1fr; gap: 20px; padding: 23px 24px 23px 70px; background: var(--cream); border-radius: 15px; counter-increment: approach-step; }
.approach-steps li::before { content: "0" counter(approach-step); position: absolute; left: 22px; top: 23px; color: #779103; font-size: .75rem; font-weight: 900; }
.approach-steps strong { color: var(--navy); }
.approach-steps span { color: var(--muted); }
.athlete-experience { background: linear-gradient(120deg, #e9eddf, var(--cream)); }
.athlete-experience-heading { max-width: 780px; margin-bottom: 45px; }
.athlete-experience-heading > p:last-child { color: var(--muted); }
.experience-list { display: grid; grid-template-columns: repeat(2, 1fr); gap: 18px; }
.experience-list article { display: flex; gap: 20px; padding: 28px; background: var(--white); border-radius: var(--radius); box-shadow: 0 10px 30px rgba(11, 23, 54, .05); }
.experience-list article > span { color: #779103; font-size: .75rem; font-weight: 900; }
.experience-list h3 { margin-bottom: 9px; color: var(--navy); }
.experience-list p { margin-bottom: 0; color: var(--muted); }
.family-partnership { display: grid; grid-template-columns: .85fr 1.15fr; gap: 9vw; background: var(--white); }
.family-partnership-copy p { color: var(--muted); }
.family-partnership-copy p:last-child { margin-bottom: 0; }
.academy-future { display: grid; grid-template-columns: 1.2fr .8fr; align-items: center; gap: 8vw; background: var(--cream); }
.academy-future-copy > p:not(.eyebrow) { color: var(--muted); }
.future-statement { display: grid; gap: 14px; padding: 38px; color: var(--navy); background: var(--lime); border-radius: var(--radius); box-shadow: var(--shadow); transform: rotate(2deg); }
.future-statement span { font-size: .72rem; font-weight: 900; letter-spacing: .11em; text-transform: uppercase; }
.future-statement strong { font-size: clamp(1.8rem, 3vw, 2.6rem); line-height: 1.08; }
.founder-promise { padding: 90px 8vw; color: var(--white); text-align: center; background: var(--blue); }
.founder-promise h2 { max-width: 850px; margin-left: auto; margin-right: auto; color: var(--white); }
.founder-promise > p:not(.eyebrow) { max-width: 780px; margin-left: auto; margin-right: auto; color: #c8d4ef; }
.about-top-actions { display: flex; flex-wrap: wrap; gap: 16px; margin-top: 28px; }
.about-top-actions .button { min-width: 180px; padding: 18px 30px; font-size: 1.05rem; transition: transform 180ms ease, box-shadow 180ms ease, color 180ms ease, background-color 180ms ease, border-color 180ms ease; }
.about-top-actions .button:hover { transform: translateY(-4px) scale(1.03); box-shadow: 0 14px 34px rgba(0, 0, 0, .24); }
.about-top-actions .button-primary:hover { color: var(--navy); background: var(--white); }
.about-top-actions .button-secondary:hover { color: var(--navy); background: var(--lime); border-color: var(--lime); }
.about-top-actions .button:focus-visible { outline: 3px solid var(--white); outline-offset: 4px; }
.plan-builder { display: grid; grid-template-columns: 1fr .8fr; align-items: start; gap: clamp(40px, 8vw, 110px); padding: 95px 8vw; background: var(--white); }
.builder-intro { color: var(--muted); }
.builder-form-panel form { display: grid; gap: 22px; margin-top: 35px; }
.builder-field { display: grid; gap: 8px; }
.builder-field label { color: var(--navy); font-weight: 850; }
.builder-field select { width: 100%; padding: 14px; color: var(--text); background: #f8fafc; border: 1px solid #d7deea; border-radius: 10px; font: inherit; outline: none; }
.builder-field select:focus { border-color: var(--blue); box-shadow: 0 0 0 3px rgba(21, 59, 142, .12); }
.builder-field small { color: var(--muted); }
.range-field { margin-top: 6px; }
.range-label-row { display: flex; align-items: center; justify-content: space-between; gap: 18px; }
.range-label-row output { min-width: 92px; padding: 8px 12px; color: var(--navy); text-align: center; background: var(--lime); border-radius: 999px; font-size: .9rem; font-weight: 900; }
.range-field input[type="range"] { width: 100%; height: 30px; margin: 5px 0 0; accent-color: var(--blue); cursor: pointer; }
.range-field input[type="range"]:focus-visible { outline: 3px solid rgba(21, 59, 142, .24); outline-offset: 4px; border-radius: 999px; }
.range-scale { display: flex; justify-content: space-between; margin-top: -4px; color: var(--muted); font-size: .75rem; font-weight: 750; }
.custom-summary { position: sticky; top: 110px; display: grid; gap: 15px; padding: clamp(28px, 4vw, 42px); color: var(--white); background: var(--navy); border-radius: var(--radius); box-shadow: var(--shadow); }
.summary-label { margin-bottom: 2px; color: var(--lime); font-size: .72rem; font-weight: 900; letter-spacing: .1em; text-transform: uppercase; }
.summary-line, .summary-savings, .summary-total, .summary-discount { display: flex; align-items: center; justify-content: space-between; gap: 20px; }
.summary-line, .summary-savings { padding-bottom: 12px; color: #b9c5d9; border-bottom: 1px solid rgba(255, 255, 255, .12); }
.summary-line strong, .summary-savings strong { color: var(--white); }
.summary-discount { padding: 13px 15px; color: var(--navy); background: var(--lime); border-radius: 12px; }
.summary-total { padding-top: 8px; }
.summary-total strong { color: var(--lime); font-size: 2rem; }
.payment-options { display: grid; gap: 9px; min-width: 0; margin: 8px 0 0; padding: 20px; border: 1px solid rgba(255, 255, 255, .14); border-radius: 18px; }
.payment-options legend { padding: 0 8px; color: var(--white); font-size: 1.05rem; font-weight: 900; }
.payment-option { display: grid; grid-template-columns: auto 1fr; align-items: center; gap: 12px; padding: 11px 12px; color: var(--white); background: rgba(255, 255, 255, .055); border: 1px solid transparent; border-radius: 13px; cursor: pointer; transition: background 170ms ease, border-color 170ms ease, transform 170ms ease, opacity 170ms ease; }
.payment-option:hover { background: rgba(199, 241, 52, .1); border-color: rgba(199, 241, 52, .32); transform: translateX(4px); }
.payment-option input { display: grid; place-content: center; width: 22px; height: 22px; margin: 0; appearance: none; color: var(--navy); background: var(--white); border: 2px solid #8290aa; border-radius: 7px; cursor: pointer; transition: background 160ms ease, border-color 160ms ease, box-shadow 160ms ease, transform 160ms ease; }
.payment-option input::before { content: "\2713"; font-size: .9rem; font-weight: 950; line-height: 1; transform: scale(0); transition: transform 140ms ease; }
.payment-option input:checked { background: var(--lime); border-color: var(--lime); box-shadow: 0 0 0 4px rgba(199, 241, 52, .12); transform: scale(1.06); }
.payment-option input:checked::before { transform: scale(1); }
.payment-option input:focus-visible { outline: 3px solid var(--lime); outline-offset: 4px; }
.payment-option > span { display: grid; gap: 1px; }
.payment-option strong { font-size: .9rem; }
.payment-option small { color: #9facbf; font-size: .72rem; line-height: 1.35; }
.payment-option-major { padding: 15px 14px; background: rgba(199, 241, 52, .08); border-color: rgba(199, 241, 52, .2); }
.payment-option-major input { width: 30px; height: 30px; border-radius: 9px; }
.payment-option-major input::before { font-size: 1.15rem; }
.payment-option-major strong { color: var(--lime); font-size: 1.02rem; }
.payment-option:has(input:disabled) { opacity: .35; cursor: not-allowed; filter: grayscale(.5); transform: none; }
.payment-option input:disabled { cursor: not-allowed; }
.payment-status { margin: 5px 0 0; padding: 9px 11px; color: #bac6da; background: rgba(255, 255, 255, .05); border-radius: 10px; font-size: .76rem; line-height: 1.45; }
.summary-cashback { display: flex; align-items: center; justify-content: space-between; gap: 18px; padding: 12px 14px; color: var(--navy); background: var(--lime); border-radius: 12px; font-weight: 900; }
.summary-cashback[hidden] { display: none; }
.cashback-note { margin: 0; padding: 14px; color: #cbd5e1; background: rgba(199, 241, 52, .08); border-left: 3px solid var(--lime); border-radius: 0 10px 10px 0; font-size: .8rem; line-height: 1.5; }
.cashback-note strong { color: var(--lime); }
.custom-register-button { margin-top: 8px; color: var(--navy); background: var(--lime); }
.custom-register-button.disabled { opacity: .45; cursor: not-allowed; }
.custom-summary > small { color: #9cabc4; line-height: 1.5; }
.discount-rules { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1px; padding: 1px; color: var(--white); background: #31436b; }
.discount-rules article { display: grid; gap: 3px; padding: 30px; text-align: center; background: var(--blue); }
.discount-rules strong { color: var(--lime); font-size: 1.2rem; }
.discount-rules span { color: #c8d4ef; font-size: .85rem; }

/* How We Work page */
.how-work-page { background: var(--cream); }
.work-page-progress { position: absolute; right: 0; bottom: 0; left: 0; height: 3px; background: rgba(255, 255, 255, .08); }
.work-page-progress span { display: block; width: 0; height: 100%; background: var(--lime); box-shadow: 0 0 14px rgba(199, 241, 52, .8); transition: width 80ms linear; }
.work-hero { display: grid; grid-template-columns: 1.15fr .85fr; align-items: center; gap: clamp(50px, 8vw, 120px); min-height: calc(92vh - 76px); padding: 100px 7vw; color: var(--white); background: radial-gradient(circle at 83% 24%, rgba(199, 241, 52, .18), transparent 24%), linear-gradient(135deg, var(--navy), #102e70); overflow: hidden; }
.work-hero-copy { max-width: 800px; }
.work-hero h1 { font-size: clamp(3.4rem, 7vw, 6.6rem); }
.work-hero-copy > p:not(.eyebrow) { max-width: 710px; color: #cbd5e1; font-size: clamp(1rem, 1.8vw, 1.18rem); }
.work-hero-actions { display: flex; flex-wrap: wrap; gap: 14px; margin-top: 32px; }
.work-promise-card { position: relative; display: grid; gap: 18px; padding: clamp(30px, 5vw, 52px); color: var(--navy); background: var(--lime); border-radius: 34px 34px 34px 8px; box-shadow: 0 30px 70px rgba(0, 0, 0, .24); transform: rotate(2deg); }
.work-promise-card::before { content: "PEAK"; position: absolute; right: 20px; bottom: -12px; color: rgba(11, 23, 54, .08); font-size: clamp(4rem, 8vw, 7rem); font-weight: 950; letter-spacing: -.08em; pointer-events: none; }
.promise-kicker { font-size: .7rem; font-weight: 950; letter-spacing: .15em; text-transform: uppercase; }
.work-promise-card > strong { position: relative; font-family: "Trebuchet MS", Inter, sans-serif; font-size: clamp(1.7rem, 3vw, 2.5rem); line-height: 1.12; letter-spacing: -.04em; }
.work-promise-card ul { position: relative; display: grid; gap: 10px; margin: 0; padding: 0; list-style: none; font-weight: 800; }
.work-promise-card li::before { content: "\2713"; display: inline-grid; place-items: center; width: 24px; height: 24px; margin-right: 9px; color: var(--lime); background: var(--navy); border-radius: 50%; font-size: .75rem; }
.work-metrics { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; padding: 1px; color: var(--white); background: #304269; }
.work-metrics article { display: grid; gap: 3px; padding: 32px 6vw; background: var(--blue); }
.work-metrics strong { color: var(--lime); font-family: "Arial Black", Inter, sans-serif; font-size: clamp(2rem, 4vw, 3.5rem); line-height: 1; letter-spacing: -.05em; }
.work-metrics article > span { color: #c3cee2; font-size: .78rem; font-weight: 800; letter-spacing: .07em; text-transform: uppercase; }
.work-process-section, .training-quality-section, .work-faq-section { padding: 100px 7vw; }
.work-section-heading { max-width: 760px; margin-bottom: 50px; }
.work-section-heading > p:last-child { color: var(--muted); }
.work-process-layout { display: grid; grid-template-columns: .75fr 1.25fr; gap: clamp(28px, 6vw, 80px); align-items: stretch; }
.work-step-list { display: grid; gap: 12px; }
.work-step-button { display: grid; grid-template-columns: auto 1fr; gap: 1px 16px; padding: 20px; color: var(--navy); text-align: left; background: var(--white); border: 1px solid rgba(21, 59, 142, .1); border-radius: 17px; box-shadow: 0 8px 25px rgba(11, 23, 54, .05); font: inherit; cursor: pointer; transition: color 180ms ease, background 180ms ease, border-color 180ms ease, box-shadow 180ms ease, transform 180ms ease; }
.work-step-button > span { grid-row: 1 / span 2; display: grid; place-items: center; width: 42px; height: 42px; color: #6f850b; background: #eff6d5; border-radius: 13px; font-size: .7rem; font-weight: 950; }
.work-step-button strong { font-family: "Trebuchet MS", Inter, sans-serif; font-size: 1.08rem; letter-spacing: -.02em; }
.work-step-button small { color: var(--muted); }
.work-step-button:hover { border-color: rgba(119, 145, 3, .45); box-shadow: 0 14px 34px rgba(11, 23, 54, .1); transform: translateX(5px); }
.work-step-button.is-active { color: var(--white); background: var(--blue); border-color: var(--blue); box-shadow: 0 18px 40px rgba(21, 59, 142, .2); }
.work-step-button.is-active > span { color: var(--navy); background: var(--lime); }
.work-step-button.is-active small { color: #c5d1e6; }
.work-step-button:focus-visible { outline: 3px solid var(--lime); outline-offset: 3px; }
.work-step-detail { position: relative; display: flex; flex-direction: column; justify-content: center; min-height: 470px; padding: clamp(34px, 6vw, 70px); overflow: hidden; color: var(--white); background: radial-gradient(circle at 85% 20%, rgba(199, 241, 52, .17), transparent 28%), var(--navy); border-radius: 28px; box-shadow: var(--shadow); }
.work-step-detail::after { content: ""; position: absolute; right: -70px; bottom: -70px; width: 230px; height: 230px; border: 34px solid rgba(199, 241, 52, .08); border-radius: 50%; }
.detail-step { position: relative; z-index: 1; color: var(--lime); font-size: .72rem; font-weight: 950; letter-spacing: .15em; text-transform: uppercase; }
.work-step-detail h3 { position: relative; z-index: 1; max-width: 650px; margin: 14px 0; color: var(--white); font-family: "Trebuchet MS", Inter, sans-serif; font-size: clamp(2rem, 4vw, 3.4rem); line-height: 1.06; letter-spacing: -.05em; }
.work-step-detail > p { position: relative; z-index: 1; max-width: 720px; color: #c6d1e4; }
.detail-outcome { position: relative; z-index: 1; display: grid; gap: 3px; margin-top: 24px; padding: 17px 19px; color: var(--navy); background: var(--lime); border-radius: 14px; }
.detail-outcome span { font-size: .65rem; font-weight: 950; letter-spacing: .12em; text-transform: uppercase; }
.detail-updated { animation: detail-pop 300ms ease; }
@keyframes detail-pop { 50% { transform: scale(.985); } }
.customer-care-section { display: grid; grid-template-columns: .85fr 1.15fr; gap: clamp(50px, 8vw, 120px); padding: 100px 7vw; background: var(--white); }
.customer-care-copy { align-self: center; }
.customer-care-copy > p:not(.eyebrow) { color: var(--muted); }
.text-action { display: inline-flex; align-items: center; gap: 9px; margin-top: 14px; color: #667d06; font-weight: 900; }
.text-action span { transition: transform 160ms ease; }
.text-action:hover span { transform: translateX(6px); }
.care-promise-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; }
.care-promise { padding: 27px; background: var(--cream); border: 1px solid #e5e7df; border-radius: 20px; transition: border-color 180ms ease, box-shadow 180ms ease, transform 180ms ease; }
.care-promise:hover { border-color: var(--lime); box-shadow: 0 18px 38px rgba(11, 23, 54, .09); transform: translateY(-6px); }
.care-promise > span { color: #758d08; font-size: .7rem; font-weight: 950; letter-spacing: .12em; }
.care-promise h3 { margin: 22px 0 8px; color: var(--navy); font-family: "Trebuchet MS", Inter, sans-serif; font-size: 1.3rem; }
.care-promise p { margin: 0; color: var(--muted); font-size: .9rem; }
.training-quality-section { background: linear-gradient(145deg, #eef1e6, var(--cream)); }
.training-quality-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 18px; }
.quality-card { position: relative; padding: 29px; overflow: hidden; background: var(--white); border-radius: 20px; box-shadow: 0 9px 30px rgba(11, 23, 54, .055); transition: box-shadow 180ms ease, transform 180ms ease; }
.quality-card:hover { box-shadow: 0 20px 42px rgba(11, 23, 54, .12); transform: translateY(-7px); }
.quality-card > span { display: grid; place-items: center; width: 46px; height: 46px; color: var(--navy); background: var(--lime); border-radius: 50% 50% 50% 12px; font-family: "Arial Black", Inter, sans-serif; font-size: .8rem; transition: transform 180ms ease; }
.quality-card:hover > span { transform: rotate(-8deg) scale(1.08); }
.quality-card h3 { margin: 24px 0 9px; color: var(--navy); font-family: "Trebuchet MS", Inter, sans-serif; font-size: 1.25rem; }
.quality-card p { margin: 0; color: var(--muted); font-size: .9rem; }
.flexibility-section { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(50px, 8vw, 110px); align-items: center; padding: 100px 7vw; color: var(--white); background: var(--blue); }
.flexibility-copy h2 { color: var(--white); }
.flexibility-copy > p:not(.eyebrow) { color: #c6d1e4; }
.schedule-choices { display: flex; flex-wrap: wrap; gap: 9px; margin-top: 28px; }
.schedule-choice { padding: 10px 14px; color: var(--white); background: rgba(255, 255, 255, .08); border: 1px solid rgba(255, 255, 255, .22); border-radius: 999px; font: inherit; font-size: .82rem; font-weight: 800; cursor: pointer; transition: color 160ms ease, background 160ms ease, border-color 160ms ease, transform 160ms ease; }
.schedule-choice:hover { border-color: var(--lime); transform: translateY(-2px); }
.schedule-choice.is-active { color: var(--navy); background: var(--lime); border-color: var(--lime); box-shadow: 0 8px 24px rgba(199, 241, 52, .2); }
.schedule-choice:focus-visible { outline: 3px solid var(--lime); outline-offset: 3px; }
.schedule-preview { position: relative; padding: clamp(34px, 5vw, 54px); color: var(--navy); background: var(--lime); border-radius: 8px 30px 30px 30px; box-shadow: 0 24px 60px rgba(0, 0, 0, .2); }
.schedule-preview > span { font-size: .68rem; font-weight: 950; letter-spacing: .13em; text-transform: uppercase; }
.schedule-preview h3 { margin: 15px 0 12px; font-family: "Trebuchet MS", Inter, sans-serif; font-size: clamp(1.8rem, 3vw, 2.7rem); line-height: 1.08; letter-spacing: -.045em; }
.schedule-preview p { color: #34410c; }
.schedule-preview .button { margin-top: 12px; color: var(--white); background: var(--navy); }
.work-faq-section { background: var(--white); }
.work-accordion { display: grid; gap: 12px; max-width: 960px; }
.work-accordion-item { overflow: hidden; background: var(--cream); border: 1px solid #e2e5da; border-radius: 17px; }
.work-accordion-item button { display: flex; align-items: center; justify-content: space-between; gap: 30px; width: 100%; padding: 22px 24px; color: var(--navy); text-align: left; background: transparent; border: 0; font: inherit; font-weight: 900; cursor: pointer; }
.work-accordion-item button > span:last-child { display: grid; flex: 0 0 auto; place-items: center; width: 30px; height: 30px; color: var(--navy); background: var(--lime); border-radius: 50%; font-size: 1.15rem; }
.work-accordion-item button:hover { color: #617606; }
.work-accordion-item button:focus-visible { outline: 3px solid var(--blue); outline-offset: -4px; }
.work-accordion-answer { padding: 0 24px 22px; }
.work-accordion-answer p { max-width: 800px; margin: 0; color: var(--muted); }
.work-final-cta { display: flex; align-items: center; justify-content: space-between; gap: 50px; padding: 85px 7vw; color: var(--white); background: radial-gradient(circle at 82% 30%, rgba(199, 241, 52, .18), transparent 22%), var(--navy); }
.work-final-cta h2 { max-width: 820px; margin: 0; color: var(--white); }
.work-final-actions { display: flex; flex: 0 0 auto; flex-wrap: wrap; gap: 12px; }
.reveal-item { opacity: 0; transform: translateY(24px); transition: opacity 520ms ease, transform 520ms ease; }
.reveal-item.is-visible { opacity: 1; transform: translateY(0); }

@media (max-width: 1100px) and (min-width: 821px) {
  .menu-toggle { display: block; }
  .main-nav { position: absolute; top: 76px; left: 0; right: 0; display: none; flex-direction: column; align-items: stretch; gap: 0; padding: 12px 6vw 24px; background: var(--navy); }
  .main-nav.open { display: flex; }
  .main-nav a:not(.nav-cta) { justify-content: flex-start; padding: 13px 10px; }
  .nav-cta { margin-top: 8px; text-align: center; }
}

@media (prefers-reduced-motion: reduce) {
  .reveal-item { opacity: 1; transform: none; transition: none; }
  .detail-updated { animation: none; }
  .work-page-progress span { transition: none; }
}

@media (max-width: 820px) {
  .menu-toggle { display: block; }
  .main-nav { position: absolute; top: 76px; left: 0; right: 0; display: none; flex-direction: column; align-items: stretch; gap: 0; padding: 12px 6vw 24px; background: var(--navy); }
  .main-nav.open { display: flex; }
  .main-nav a { padding: 13px 0; }
  .main-nav a:not(.nav-cta) { justify-content: flex-start; padding: 13px 10px; }
  .nav-cta { margin-top: 8px; text-align: center; }
  .hero { grid-template-columns: 1fr; padding-top: 80px; }
  .hero-visual { min-height: 330px; }
  .program-grid { grid-template-columns: 1fr; }
  .program-selection { align-items: flex-start; flex-direction: column; }
  .about { grid-template-columns: 1fr; gap: 24px; }
  .contact { grid-template-columns: 1fr; }
  .contact-intro { position: static; }
  .faq-content-heading { align-items: start; flex-direction: column; gap: 4px; }
  .faq-cta { align-items: flex-start; flex-direction: column; }
  .pricing-grid { grid-template-columns: repeat(2, 1fr); }
  .pricing-card.featured-plan { transform: none; }
  .pricing-card.featured-plan:hover { transform: translateY(-5px); }
  .checkout-section { grid-template-columns: 1fr; }
  .custom-plan-cta { align-items: flex-start; flex-direction: column; }
  .custom-plan-hero, .plan-builder { grid-template-columns: 1fr; }
  .custom-hero-stat { transform: none; }
  .custom-summary { position: static; }
  .discount-rules { grid-template-columns: repeat(2, 1fr); }
  .about-me-hero, .founder-story, .about-approach { grid-template-columns: 1fr; }
  .founder-card { max-width: 520px; transform: none; }
  .academy-principles { grid-template-columns: repeat(2, 1fr); }
  .founder-vision-grid { grid-template-columns: 1fr; }
  .family-partnership, .academy-future { grid-template-columns: 1fr; }
  .future-statement { max-width: 600px; transform: none; }
  footer { flex-direction: column; text-align: center; }
}

@media (max-width: 520px) {
  .site-header { padding-inline: 20px; }
  .logo > span:last-child { font-size: .9rem; }
  .hero, .section, .contact { padding-left: 22px; padding-right: 22px; }
  .hero-stats { justify-content: space-between; gap: 12px; }
  .hero-stats strong { font-size: 1.25rem; }
  .program-card { min-height: 0; }
  .program-selection .button { width: 100%; white-space: normal; }
  .hero-visual { min-height: 260px; }
  .visual-card { right: 14px; }
  .form-row { grid-template-columns: 1fr; }
  .form-submit { width: 100%; }
  .coach-launcher { right: 16px; bottom: 16px; }
  .coach-assistant { right: 16px; bottom: 78px; height: min(570px, calc(100vh - 100px)); }
  .faq-hero, .faq-content, .faq-cta { padding-left: 22px; padding-right: 22px; }
  .faq-search span { display: none; }
  .faq-question { padding: 18px; }
  .faq-answer { padding: 0 18px 18px; }
  .pricing-hero, .pricing-section, .checkout-section { padding-left: 22px; padding-right: 22px; }
  .pricing-grid { grid-template-columns: 1fr; }
  .custom-plan-hero, .discount-banner-section, .plan-builder, .custom-plan-cta { padding-left: 22px; padding-right: 22px; }
  .discount-rules { grid-template-columns: 1fr; }
  .discount-banner-fallback { grid-template-columns: 1fr; }
  .discount-banner-fallback article { min-height: 240px; }
  .about-me-hero, .founder-story, .founder-vision, .academy-story, .about-approach, .athlete-experience, .family-partnership, .academy-future, .founder-promise { padding-left: 22px; padding-right: 22px; }
  .about-top-actions .button { width: 100%; }
  .academy-principles { grid-template-columns: 1fr; }
  .approach-steps li { grid-template-columns: 1fr; gap: 4px; }
  .experience-list { grid-template-columns: 1fr; }
}

@media (max-width: 820px) {
  .work-hero, .customer-care-section, .flexibility-section { grid-template-columns: 1fr; }
  .work-hero { min-height: auto; padding-top: 78px; }
  .work-promise-card { max-width: 610px; transform: none; }
  .work-process-layout { grid-template-columns: 1fr; }
  .work-step-detail { min-height: 390px; }
  .training-quality-grid { grid-template-columns: repeat(2, 1fr); }
  .work-final-cta { align-items: flex-start; flex-direction: column; }
}

@media (max-width: 520px) {
  .work-hero, .work-process-section, .customer-care-section, .training-quality-section, .flexibility-section, .work-faq-section, .work-final-cta { padding-left: 22px; padding-right: 22px; }
  .work-hero h1 { font-size: clamp(3rem, 15vw, 4.4rem); }
  .work-hero-actions .button, .work-final-actions .button { width: 100%; }
  .work-metrics { grid-template-columns: 1fr; }
  .care-promise-grid, .training-quality-grid { grid-template-columns: 1fr; }
  .work-step-button { padding: 17px; }
  .work-step-detail { min-height: 0; padding: 30px 24px; }
  .schedule-choices { display: grid; grid-template-columns: 1fr 1fr; }
  .schedule-choice { width: 100%; }
  .work-final-actions { width: 100%; }
}

@media (prefers-reduced-motion: reduce) {
  .site-interactive, .site-interactive:hover, .site-interactive.is-interaction-pressed, .site-interactive.is-key-activated { scale: 1; animation: none; transition: none; }
  .site-interaction-layer { display: none; }
}
